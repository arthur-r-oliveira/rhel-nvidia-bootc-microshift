# Declared for bootc container lint (sysusers check). systemd-sysusers runs in Containerfile.
# GID/UID 1000 must stay aligned with the systemd-sysusers + chpasswd step there.
g redhat 1000
u redhat 1000:1000 "MicroShift local user" /var/home/redhat
m redhat wheel
